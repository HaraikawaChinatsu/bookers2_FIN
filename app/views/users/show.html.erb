<div class="body">
      <div class="container">
      	<div class="row">
<div class="col-xs-3">
    <h2>User info</h2>

    <table class="table">
      <thead>
        <tr>image</tr>
     <%= attachment_image_tag @user, :profile_image, :fill, 100, 100, format: 'jpeg' %>
     <!--%= image_tag @user.no_image.jpg, class:"user-icon" %>-->

     </thead>
      <tbody>
        <tr>
          <td>name</td>
          <td><%= current_user.name %></td>
        </tr>
        <tr>
          <td>introduction</td>
          <td><%= current_user.introduction %></td>
    	  </tr>
    	</tbody>
    </table>


<div class="row">
<!--%= link_to "編集", edit_user_registration_path %>-->
</div>
<a class="col-xs-12 btn btn-default glyphicon glyphicon-wrench edit_user_26053" href="/users/edit"></a>

<h2>New Book</h2>

<% if @book.errors.any? %>
<!--@bookの中にエラーある？-->
  <%= @book.errors.count %>errors
  <% @book.errors.full_messages.each do |message| %>
<!--エラーに対する表示は決まっている-->
  <%= message %>
  <% end %>
<% end %>

<!-- form_with部分 -->
<%= form_with model: @book, local:true do |f| %>
  <h4>title</h4>
  <%= f.text_field :title %>
  <h4>body</h4>
  <%= f.text_area :body %>
<div class="actions row">
<input type="submit" name="commit" value="Create Book" class="btn btn-primary col-xs-12" data-disable-with="Create Book">
</div>

<% end %>

  <div class="actions row">
    <!--<input type="submit" name="commit" value="Create Book" class="btn btn-primary col-xs-12" data-disable-with="Create Book">-->
  </div>
</form>
  </div>
  <div class="col-xs-9">
		<h2>Books</h2>
		<table class="table">
		  <tbody><tr>
        <td>
        <!--<a href="/users/25909">-->
   <thead>
     <tr>
      <th>image</th>
      <th>Title</th>
      <th>Opinion</th>
     </tr>
   </thead>
   <tbody>
<% @books.each do |book| %>
   <tr>
     <td> image </td>
     <td><%= book.title %></td>
     <td><%= book.title %></td>
   </tr>
<% end %>
   </tbody>

  </div>
</div>
</div>
</div>
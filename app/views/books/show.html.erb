
<div class="body">
 <div class="container">
   <div class="row">
     <div class="col-xs-3">
      <h2>User info</h2>
      <table class="table">
        <thead>
          <tr>
            <td><%= attachment_image_tag @book.user, :profile_image, fallback:"no_image.jpg" ,size:"100x100" %></td>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>name</td>
            <td><%= @user.name %></td>
          </tr>
          <tr>
            <td>introduction</td>
            <td><%= @user.introduction %></td>
          </tr>
        </tbody>
      </table>


<div class="row">
</div>
<%= link_to "" , edit_user_path(@user), class: "col-xs-12 btn btn-default glyphicon glyphicon-wrench " %>


<h2>New book</h2>
  <% if @book.errors.any? %>
    <%= @book.errors.count %>errors
    <% @book.errors.full_messages.each do |message| %>
    <%= message %>
    <% end %>
  <% end %>
<!-- form_with部分 -->
 <%= form_with model: @book, local:true do |f| %>
  <h4>title</h4>
    <%= f.text_field :title , class:"col-xs-12 book_title" %>
  <h4>opinion</h4>
    <%= f.text_area :body , class:"col-xs-12 book_body" %>
<div class="actions row">
 <div class="text-center">
    <%= f.submit 'Create Book', class: 'btn btn-primary col-xs-12' %>
 </div>
</div>

<% end %>


  <div class="actions row">
  </div>
</form>
  </div>
  <div class="col-xs-9">
		<h2>Book detail</h2>
		<table class="table">
		  <tbody><tr>
   <tbody>
   <tr>
     <td>
         <%= link_to user_path(@book.user_id) do %>
         <%= attachment_image_tag @book.user, :profile_image, fallback:"no_image.jpg" ,size:"50x50" %>
         <%= @book.user.name %>
         <% end %>
         </td>

         <!--%= attachment_image_tag @book.user, :profile_image, fallback:"no_image.jpg" ,size:"50x50" %></td>-->
     <td><%= @book.title %></td>
     <td><%= @book.body %></td>

     <td>
     <% if @book.user == current_user %>
     <%= link_to "Edit", edit_book_path(@book), class: 'btn-sm btn-success' %>
     <!--%= link_to "削除", post_image_path(@post_image), method: :delete %>-->
     <% end %>

     <td>
     <% if @book.user == current_user %>
     <%= link_to "Destroy", book_path(@book), method: :delete, class: 'btn-sm btn-danger' , data: { confirm: '本当に削除しますか？' } %></td>
     <% end %>
   </tr>
   </tbody>
  </div>